import {PingCallback} from '../../src/types/PingCallback';
import {expect} from 'chai';

describe('types/PingCallback', (): void => {

  it('has a stub unit test', (done: () => void): void => {

    expect(true).to.be.true;
    done();

  });

});
