import {BooleanVector} from '../../src/types/BooleanVector';
import {expect} from 'chai';

describe('BooleanVector', (): void => {

  it('has a stub unit test', (done: () => void): void => {

    expect(true).to.be.true;
    done();

  });

});
