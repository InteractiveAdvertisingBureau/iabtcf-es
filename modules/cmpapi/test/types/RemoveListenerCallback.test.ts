import {RemoveListenerCallback} from '../../src/types/RemoveListenerCallback';
import {expect} from 'chai';

describe('types/RemoveListenerCallback', (): void => {

  it('has a stub unit test', (done: () => void): void => {

    expect(true).to.be.true;
    done();

  });

});
